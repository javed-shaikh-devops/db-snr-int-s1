apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: {{ .Values.casIssuer.issuerName }}
spec:
  googleCAS:
    project: {{ .Values.casIssuer.projectId }}
    location: {{ .Values.casIssuer.location }}
    caPool: {{ .Values.casIssuer.caPool }}